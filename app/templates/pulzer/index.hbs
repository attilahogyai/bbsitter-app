{{partial 'pulzer/menu'}}
<div style="width:100%" id="container">
	<div id="d{{ub-date-f actDate 'YYYYMMDD'}}">
	{{#day-events date=actDate new="createEvent" next="jumpToNext" prev="jumpToPrev" settings=settings valueSelected=citySelected}}
	{{#each eventList}}
	{{view "event" event=this}}
	{{/each}}
	{{/day-events}}
	</div>
</div>
